function classifyPhoneNumber(phoneNumber: string[]) {
    let viettel: string[] = [];
    let mobifone: number[] = [];
    let vinaphone: number[] = [];

    for (let i of phoneNumber) {
        switch (phoneNumber[i].sub) {
            case 086:

        }
    }
}